---
parseable: true
skip_list:
  # Intensive use of the command module, can't always use changed_when for now.
  # This will be improve in the futur.
   - no-changed-when
   - meta-no-info # Not intended for galaxy, os that's ok if meta/main miss informations
